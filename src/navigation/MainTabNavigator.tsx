// Este arquivo está completamente desativado para remover o BottomTabNavigator antigo

import React from 'react';
import { View } from 'react-native';

const MainTabNavigator = () => {
  // Retorna um componente vazio para que não haja erros de importação
  return null;
};

export default MainTabNavigator; 